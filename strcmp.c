#include<stdio.h>
#include<string.h>
int main()
    {
        char firststr[]="Apple";
        char secondstr[]="Banana";
        printf("%d",strcmp(firststr,secondstr));
        puts(firststr);
        puts(secondstr);
        return 0;
    }